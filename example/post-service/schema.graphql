schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key", "@shareable"])
{
  query: Query
}

type Post {
  id: Int!
  title: String!
  content: String!
  userId: Int!
}

type Query {
  posts: [Post!]
  post(id: Int!): Post
}